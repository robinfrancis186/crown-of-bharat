{
  "name": "monsoon-kingdoms-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 5191 --bind 127.0.0.1",
    "test": "npm --prefix monsoon-kingdoms test",
    "build": "npm --prefix monsoon-kingdoms run build"
  },
  "dependencies": {
    "firebase": "^12.19.0",
    "three": "0.169.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
